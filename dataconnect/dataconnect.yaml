specVersion: "v1"
serviceId: "coral-web-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "coral-web-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
